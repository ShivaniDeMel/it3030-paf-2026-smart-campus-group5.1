# Server Configuration
server.port=8089

# MongoDB Configuration
# Replace with your actual MongoDB URI
# Format: mongodb+srv://username:password@cluster.mongodb.net/database_name?retryWrites=true&w=majority&appName=ClusterName
spring.data.mongodb.uri=mongodb+srv://admin:PAF123@cluster0.xe6gwon.mongodb.net/smart_campus?appName=Cluster0

# Logging Configuration
logging.level.com.smartcampus=DEBUG
logging.level.org.springframework.data.mongodb=DEBUG

# JWT Configuration
# Generate a secure secret key for production (at least 256 bits)
jwt.secret=mySecretKey123456789012345678901234567890
jwt.expiration=86400000

# CORS Configuration
# Update with your frontend URLs
spring.web.cors.allowed-origins=http://localhost:3000,http://localhost:5173
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true
